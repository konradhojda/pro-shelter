import Container from "../../Container/Container";
import Flex from "../../Flex/Flex";

export const Footer = () => {
    return (
        <footer>
            <Container>
                <Flex>footer</Flex>
            </Container>
        </footer>
    );
};

export default Footer;
